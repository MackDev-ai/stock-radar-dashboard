name: Dashboard UI Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "monitoring-dashboard.html"
      - "stock-map.html"
      - "scripts/build-online-dashboard.js"
      - "scripts/restore-public-data.js"
      - "scripts/check-data-contract.js"
      - "monitoring-config.json"
      - ".github/workflows/dashboard-ui.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: stock-radar-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DASHBOARD_URL: ${{ vars.DASHBOARD_URL }}
      CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
      CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          package-manager-cache: false

      - name: Restore latest public runtime data
        env:
          RESTORE_PUBLIC_DATA_MODE: refresh
        run: node scripts/restore-public-data.js

      - name: Build static site from restored data
        run: node scripts/build-online-dashboard.js

      - name: Check data contract
        env:
          REQUIRE_CANONICAL_DECISIONS: "false"
        run: node scripts/check-data-contract.js

      - name: Check dashboard UI
        run: node scripts/check-dashboard-ui.js

      - name: Configure Pages
        if: ${{ vars.ENABLE_GITHUB_PAGES != 'false' }}
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        if: ${{ vars.ENABLE_GITHUB_PAGES != 'false' }}
        uses: actions/upload-pages-artifact@v5
        with:
          path: site-dist

  deploy:
    needs: build
    if: ${{ vars.ENABLE_GITHUB_PAGES != 'false' }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v5
